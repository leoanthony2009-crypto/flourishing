{
  "name": "bloom-principal-pulse",
  "private": true,
  "description": "Weekly principal check-in for the five Bloom Academy schools in Trinidad & Tobago.",
  "scripts": {
    "check": "python3 build.py --verify && python3 build.py --check && node checks.mjs && node tailor-compose.mjs",
    "build": "python3 build.py",
    "verify-build": "python3 build.py --verify",
    "check-bundle": "python3 build.py --check",
    "serve": "python3 -m http.server 8765 --directory deploy",
    "smoke": "node smoke.mjs && node inbox-smoke.mjs && node inbox-render.mjs && node rescue.mjs && node link-safety.mjs && node link-error.mjs && node staff-signin.mjs && node app-signed-in.mjs && node sheets.mjs && node timezone.mjs",
    "e2e": "node e2e.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.1"
  }
}
